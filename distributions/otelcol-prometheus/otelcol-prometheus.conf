# Systemd environment file for the otelcol-prometheus service

# Command-line options for the otelcol-prometheus service.
# Run `/usr/bin/otelcol-prometheus --help` to see all available options.
# Note: No default config file is provided at the path below, one must be created.
OTELCOL_OPTIONS="--config=/etc/otelcol-prometheus/config.yaml"
